# Copy to .env and fill in your values
# Expo reads EXPO_PUBLIC_* at runtime

# Supabase Configuration (Required for Phase 2)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (For seed/import scripts only - DO NOT expose in app!)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Google Maps/Places API (For maps and importing location data)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Required APIs: Maps SDK for Android, Maps SDK for iOS, Places API, Places API (New), Maps JavaScript API
# IMPORTANT: This same key is used for both Google Places API (scripts) and Google Maps (native maps)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
GOOGLE_PLACES_API_KEY=your-google-places-api-key-here

# Ticketmaster Discovery API (For importing events)
# Get your FREE API key from: https://developer.ticketmaster.com/
# Sign up, create an app, and get your Consumer Key
TICKETMASTER_API_KEY=your-ticketmaster-api-key-here

# Eventbrite API (Alternative - not currently used)
# EVENTBRITE_API_TOKEN=your-eventbrite-token-here

# OpenStreetMap / Overpass (for importing places without Google)
# You can usually keep the default Overpass URL, but you MUST set a custom
# User-Agent string that identifies your app and provides contact info.
OSM_OVERPASS_URL=https://overpass-api.de/api/interpreter
OSM_USER_AGENT=SwipelyApp/1.0 (your-site-or-email-here)

# App Configuration
EXPO_PUBLIC_SITE_URL=swipely://

# Analytics (Optional - currently using console logging)
EXPO_PUBLIC_POSTHOG_KEY=
EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# Feature flags
# Set to "false" to hide OSM-sourced listings from the in-app feed
EXPO_PUBLIC_ENABLE_OSM_LISTINGS=true

